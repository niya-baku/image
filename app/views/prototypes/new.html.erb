
  <div>
    <%= form_for @prototype do |f| %>
		<%= f.fields_for :thumbnails do |thumbnail| %>
		    <%= thumbnail.file_field :image %>
		    <div class="preview" id="<%= thumbnail.index %>"></div>
		<% end %>
	    <%= f.submit "Submit" %>
    <% end %>
  </div>


  
